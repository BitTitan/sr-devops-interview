 
# {PLEASE CREATE A WORKING DOCKERFILE}
# escape=`

# Indicates that the windowsservercore image will be used as the base image.
FROM mcr.microsoft.com/windows/servercore:ltsc2019

# Create the remaing commands that will create a functional Windows Container - * required

#.  COPY* - executable from the source location to another
#.  WORKDIR* - Where the executable will run
#.  ENTRYPOINT* - Executing this command

#  Is there anything else that can be added or optimized?